---
description: List available message recipients (other agents in tmux)
argument-hint:
---

List all tmux windows in the current session that can receive messages.

Run `agentmail recipients` to see available agents. Your current window is marked with `[you]`.

Windows listed in `.agentmailignore` are excluded from the list.
